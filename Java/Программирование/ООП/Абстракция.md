Выделение ключевых характеристик объекта и игнорировании несущественных деталей. 

Позволяет описывать поведение объектов на концептуальном уровне, не привязываясь к реализации. Абстракция реализуется через абстрактные классы и интерфейсы и помогает управлять сложностью системы.

Выделение значимых характеристик объекта и отбрасывание незначимых. В моделировании мы никогда не копируем объект целиком — нам нужна только его модель, релевантная задаче.