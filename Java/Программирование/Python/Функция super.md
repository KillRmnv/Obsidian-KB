Функция на Python, при помощи которой можно вызывать методы родительского класса.

Вызывает метод класса который в [[Магический атрибут __mro__|MRO]] идёт после переданного в функцию класса.

```python
super() - эквивалентно super(CurrentClass, self)
```