**`map()`** — это промежуточная операция, которая преобразует каждый элемент потока, применяя к нему функцию. Возвращает новый поток с преобразованными элементами.

`<R> Stream<R> map(Function<? super T, ? extends R> mapper)`

![[Pasted image 20260113211351.png]]