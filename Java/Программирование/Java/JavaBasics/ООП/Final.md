[[OOP]]
`public **final** class Executive extends Manager`
`{`
`. . .`
`}`
не наследуется
`public class Employee`
`{`
`. . .`
`public final String getName()`
`{`
`return name;`
`}`
`. . .`
`}`
нельзя переписать
перменная final не может быть мутирована