1. статические блоки и статическая инициализация только для статических переменных.
2. при overloading тип возвращаемого значения не обязан совпадать и не скрывает другие полиморфные методы.
3. char 2 bytes and String character is either 1 byte(LATIN1) or 2 or 4 bytes(if string contains at least one character that have no corresponding code in LATIN1)
4. />> – сдвиг вправо на n, />>> сдвиг вправо без знака, пустые позиции заполняются нулями
5. имена интерфейсов должны быть прилагательными, а методов –главголами
6. Константы пишутся заглавными буквами
7. несколько  классов в файле, но только 1 public и имя файла должно совпадать с именем этого класса
8. [[Package]]
9. только 1 пакет в каждом файле
10. можно пропустить блок catch или блок finally, но не оба одновременно
11. блок finally нужен для освобождения ресурсов, гарантированного выполнения, обработки исключений(если catch не сделал ничего или не было подходящего можно залогировать место появления исключения), конечных операций
12. между блоком catch и try,try и finally не должно быть кода
13. можно перехватить несколько исключений с помощью одного блока catch через | , но тогда объект исключения будет final
14. при первом обращении к классу загружается статика(сначала родитель, потом ребенок).Если создается объект ребенка, то нестатические блоки, переменные и конструктор соответствующий у родителя и затем то же у ребенка
15. Если статическое поле родителя объявлено как `private`, то **напрямую из наследника к нему обращаться нельзя**, даже через `Parent.FIELD`