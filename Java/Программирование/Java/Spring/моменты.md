1. Если конструкторов несколько указываем нужный через [[@Autowired]]
2. [[@Configuration]] содержит внутри описание бинов через [[@Bean]]
3. Инджектим либо через аннотацию над полем, сеттером, Конструктором
4. Посмотерть все с ResponseEntity
5. в классической модели у нас есть пул потоков.На 1 поток-1 запрос.Решением проблемы блокировки и очередей являются переход на WebFlux,Virtual Threads, @Async, иногда можно и [[ExecutorService]] поставить для параллельных вычислений, разумеется в в сервисном слое
6. для @Modifying нужно, чтобы висело @Transactional