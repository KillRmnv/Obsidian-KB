Для собеса по сервлетам имеет смысл освежить несколько блоков: от базовой модели до сессий, фильтров и многопоточности.guru99+2

## 1. [[Базовое о сервлетах и контейнере]]

- Что такое **Servlet**, чем он отличается от CGI, зачем вообще нужен.[career.guru99](https://career.guru99.com/ru/top-50-servlet-interview-questions/)
    
- Роль **Servlet Container** (Tomcat/Jetty):
    
    - управление жизненным циклом сервлетов;
        
    - создание потоков под запросы;
        
    - маппинг URL → сервлет;
        
    - безопасность, конфигурация.javastudy+1
        
- **Структура веб‑приложения**: `WEB-INF`, `web.xml`, `META-INF`, где лежат классы и ресурсы.[javastudy](https://javastudy.ru/interview/list-of-questions-javaee-interview/)
    

## 2. [[Жизненный цикл сервлета]]

- Методы: `init()`, `service()`, `doGet()`, `doPost()`, `destroy()`.jsehelper.blogspot+1
    
- Когда вызывается `init` (один раз на инстанс), как работает `load-on-startup` (ленивая / ранняя загрузка).javastudy+1
    
- Что создаётся один раз, а что — на каждый запрос; почему **один сервлет обслуживает много потоков**.
    

## 3. [[HttpServletRequest / HttpServletResponse]]

- Основные методы `HttpServletRequest`: получение параметров, заголовков, тела (input stream/reader), атрибутов, `getRequestDispatcher()`.[youtube](https://www.youtube.com/watch?v=fSYPRPABsIM)[career.guru99](https://career.guru99.com/ru/top-50-servlet-interview-questions/)
    
- Основные методы `HttpServletResponse`: статус, заголовки, `sendRedirect`, `getWriter` / `getOutputStream`.[career.guru99](https://career.guru99.com/ru/top-50-servlet-interview-questions/)
    
- Разница **forward vs redirect** (`RequestDispatcher.forward` vs `sendRedirect`).arenda-server+1
    

## 4. Конфигурация: [[ServletConfig и ServletContext]]

- **ServletConfig**: конфиг конкретного сервлета, `init-param` из `web.xml`.javastudy+1
    
- **ServletContext**: контекст всего приложения, общие атрибуты, доступ к ресурсам, логирование.career.guru99+1
    
- Отличие `ServletConfig` vs `ServletContext` — очень типичный вопрос.javastudy+1
    

## 5. [[Сессии и состояние]]

- HTTP stateless; зачем нужно **session tracking**.guru99+1
    
- **HttpSession**: получение/создание (`request.getSession()`), время жизни, атрибуты, invalidation, session fixation.guru99+1
    
- Другие способы отслеживания сессий: cookies, URL‑rewriting, hidden fields.guru99+1
    
- Где хранятся данные сессии (на сервере), как работает `JSESSIONID`.
    

## 6. Фильтры и слушатели

- **Servlet Filter**: логирование, auth, compression, encoding, CORS и т.п..javastudy+1
    
- Цепочка фильтров (`FilterChain`), где вызывать `chain.doFilter`.[javastudy](https://javastudy.ru/interview/list-of-questions-javaee-interview/)
    
- **[[Listeners]]**:
    
    - `ServletContextListener`, `HttpSessionListener`, `ServletRequestListener` и др..[javastudy](https://javastudy.ru/interview/jee-servlet-api-questions-2/)
        
    - Для логирования жизненного цикла, ресурсов, сессий.
        

## 7. [[Потокобезопасность и shared state]]

- Один инстанс сервлета на множество потоков → **нельзя хранить request‑specific данные в полях сервлета**.jsehelper.blogspot+1
    
- Где можно хранить состояние: request attributes, session attributes, контекст приложения (и что там с конкурентным доступом).
    
- Почему `synchronized`‑поля в сервлете — почти всегда плохая идея.
    

## 8. Mapping и конфигурация

- Маппинг сервлетов: через `web.xml` или аннотацию `@WebServlet`.career.guru99+1
    
- Паттерны URL, приоритеты маппингов, `/*` vs `/path/*` vs точечные.[career.guru99](https://career.guru99.com/ru/top-50-servlet-interview-questions/)
    
- init‑params на уровне сервлета и контекста.
    

## 9. Версии Servlet API / Servlet 3+

- Основные фичи Servlet 3.x: аннотации (`@WebServlet`, `@WebFilter`, `@WebListener`), програмная регистрация, async processing.[javastudy](https://javastudy.ru/interview/jee-servlet-api-questions-2/)
    
- Что такое async servlet (`startAsync`, `AsyncContext`), в общих чертах.
    

---

Если хочешь, дальше могу:

- Развернуть каждый пункт списком «типичных вопросов» + короткий «правильный» ответ в одну‑две фразы.
    
- Или собрать для тебя мини‑шпору именно «вопрос → как отвечать на собесе» по сервлетам (штук 20–30).