Модель выполнения, при которой длительные операции запускаются **без блокировки основного потока**.

Вместо ожидания результата программа может продолжать выполнение, а обработка результата происходит позже — через события, колбэки, промисы или `async/await`. Поток управления становится нелинейным.